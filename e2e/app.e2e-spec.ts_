import { JobsPage } from './pages';

describe('workspace-project App', () => {
  let page: JobsPage;

  beforeEach(() => {
    page = new JobsPage();
  });

  it('should display welcome message', () => {
    page.navigateTo();
    expect(page.getParagraphText()).toEqual('jobs works!');
  });
});
